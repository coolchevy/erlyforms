{%block content%}
<form action="{{ form_action }}" id="{{form_name}}" class="style_for_{{form_name}}" method="post">
  <fieldset>
    <legend>{{ form_title }}</legend>
	<ul>
    {% for field in fields %}
    {{ field }}
	<span class="msg"></span>
    {% endfor %}
	</ul>
  </fieldset>
</form>
{%endblock%}
